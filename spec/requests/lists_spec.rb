require 'spec_helper'

describe "Lists" do
  it "shows lists for a user"


  describe "authorization" do
    it "returns 401 if user tries to view lists belonging to another user"
    
    it "returns 401 if user tries to delete lists belonging to another user"
  end
end